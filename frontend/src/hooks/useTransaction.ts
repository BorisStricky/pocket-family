// Placeholder hook - to be implemented in Sprint 0 Step 3
// This allows the build to succeed while we implement the actual functionality

export function useTransaction(familyId: string, transactionId?: string) {
  return {
    data: undefined,
    isLoading: false,
    error: null
  };
}
